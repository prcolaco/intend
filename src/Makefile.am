bin_PROGRAMS = intend

# set the include path found by configure
INCLUDES = -I$(top_srcdir)/libintend $(all_includes)

# the library search path.
intend_LDADD = $(top_builddir)/libintend/libintend.la
intend_SOURCES = intendcmd.c
intend_LDFLAGS = -no-undefined
